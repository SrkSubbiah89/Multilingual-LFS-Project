# Pinned environment for Module A (WISCO pipeline) — generated 2026-08-02.
#
# This module's scripts (inspect_wisco.py, analyze_wisco.py, parse_wisco.py) additionally
# import backend.rag.load_full_isco for the unit-group gap analysis, which pulls in the full
# repo requirements.txt (sentence-transformers, tf-keras, etc.) — those are pinned by the
# main repo's own dependency management, not duplicated here. This file pins only what Module
# A adds on top: reading .xlsx files, which is not otherwise a repo dependency.
#
# Reproduce with: pip install -r backend/evaluation/wisco/requirements.lock.txt
# Verified against: Python 3.11.9

openpyxl==3.1.5
et_xmlfile==2.0.0
